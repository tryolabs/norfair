[tool.poetry]
name = "norfair"
version = "0.1.0"
description = ""
authors = ["joaqo <joaquin.alori@gmail.com>"]

[tool.poetry.dependencies]
python = "^3.6"
opencv-python = "^4.2.0"
click = "^7.1.2"
filterpy = "^1.4.5"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
